[project]
name = "SERTime"
version = "0.1.0"
description = "Evaluation and Model changes"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "bandit>=1.8.6",
    "ruff>=0.14.0",
    "safety>=3.6.2",
]
